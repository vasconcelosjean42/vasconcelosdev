import styled from "styled-components";

export const Container = styled.div`
    .icon{
        width: 1.1rem;
        color: var(--primary);
        &:hover{
            color: yellow;
        } 
    }
`;